export const PAGE_DEFAULT = 1;

export const LIMIT_DEFAULT = 10;

export const PRIVATE_CHAT_MEMBERS = 2;
export const LAST_MESSAGE_LIMIT = 1;

export enum SortBy {
  NEWEST = 'newest',
  OLDEST = 'oldest',
}

export enum OrderBy {
  ASC = 'asc',
  DESC = 'desc',
}